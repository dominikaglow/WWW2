import {createContext} from "react";
//AuthContext holds user authentication data and can be accessed by any component that needs to check
// or update the authentication state.
export const AuthContext = createContext("");